[project]
name = "eventizer"
version = "0.2.0"
description = ""
authors = [
    {name = "Pete George", email = "pete@crackthedata.com"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "schedule (>=1.2.2,<2.0.0)",
    "playwright (>=1.57.0,<2.0.0)",
    "extruct (>=0.18.0,<0.19.0)",
    "beautifulsoup4 (>=4.14.3,<5.0.0)",
    "openai (>=2.15.0,<3.0.0)",
    "html2text (>=2025.4.15,<2026.0.0)",
    "python-dateutil (>=2.9.0.post0,<3.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
